= simple_form_for [:admin, @review], :html => { :class => 'form-horizontal' } do |f|
  = f.input :rate
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), admin_reviews_path, :class => 'btn'
